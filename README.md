# Numerolog AI Bot

AI-нумеролог для Telegram с персонализированными прогнозами и анализом совместимости.

## Функции

- 🔢 Расчёт нумерологического профиля (Число Судьбы, Матрица Пифагора)
- 🌅 Ежедневный прогноз на основе персонального числа дня
- 💕 Анализ совместимости
- 💬 AI-ответы на вопросы с учётом профиля
- 🌍 Мультиязычность (RU / EN)
- 💰 Freemium модель с Telegram Stars

## Технологии

- Python 3.11 + aiogram 3.x
- AWS Lambda + API Gateway (serverless)
- DynamoDB (NoSQL)
- OpenAI GPT-4o-mini

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

```bash
cp .env.example .env
# Заполните .env своими токенами
```

### 3. Локальный запуск (polling mode)

```bash
python -m src.lambda_handler
```

## Деплой в AWS

### Требования

- AWS CLI настроен (`aws configure`)
- SAM CLI установлен (`pip install aws-sam-cli`)

### Деплой

```bash
# Установите переменные окружения
export TELEGRAM_BOT_TOKEN=your_bot_token
export OPENAI_API_KEY=your_openai_key

# Запустите деплой
./scripts/deploy.sh
```

### Настройка webhook

После деплоя выполните:

```bash
./scripts/set_webhook.sh
```

Или вручную:

```bash
# Получите URL из вывода деплоя
python -m src.lambda_handler set_webhook https://your-api-gateway-url/prod/webhook
```

## Структура проекта

```
numerolog-ai/
├── src/
│   ├── handlers/
│   │   └── bot.py          # Telegram handlers
│   ├── services/
│   │   ├── ai.py           # OpenAI integration
│   │   ├── database.py     # DynamoDB operations
│   │   └── numerology.py   # Calculations
│   ├── knowledge/
│   │   └── numbers.py      # Numerology meanings
│   ├── models/
│   │   └── user.py         # Data models
│   ├── config.py           # Settings
│   ├── lambda_handler.py   # Main webhook handler
│   └── notifications_handler.py  # Daily forecasts
├── infrastructure/
│   ├── template.yaml       # SAM template
│   └── samconfig.toml      # SAM config
└── scripts/
    ├── deploy.sh           # Deployment script
    └── set_webhook.sh      # Webhook setup
```

## Тарифы

| Тариф | Цена | Возможности |
|-------|------|-------------|
| FREE | 0 | 3 вопроса/день |
| LITE | 175★ | Безлимит вопросов |
| PRO | 500★ | Безлимит + все отчёты |

Премиум-отчёты: 100★ каждый

## Команды бота

- `/start` — Начало работы / онбординг
- `/profile` — Нумерологический портрет
- `/today` — Прогноз на сегодня
- `/compatibility` — Анализ совместимости
- `/buy` — Покупка подписки
- `/invite` — Реферальная программа
- `/help` — Помощь

## Лицензия

MIT
